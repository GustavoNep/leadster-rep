import styled from "styled-components";

const AnouLine = styled.div`
  border-bottom: 2px solid rgba(128, 128, 128, 0.3);
`;


export default function AnnouLine() {
    return(
        <AnouLine></AnouLine>
    )
}